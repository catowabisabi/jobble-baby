fix: implement salary nudge comparator with visual anchor

## Context
#15 is a new P3 task. Build salary nudge comparator that shows users where they stand vs market range for their role/experience, with clear visual indicator if they are below/above median. Based on idea #2 (Nudge-based salary comparator).

## Task

### Step 1: Read existing salary query
Look at:
- src/app/salary-query.tsx
- backend/app/api/salary.py (if exists)

Understand current salary query UI and data model.

### Step 2: Design backend
Create or extend salary API to return:
- Market range (low, median, high) for user's role/experience/location
- User's target salary position relative to market
- Visual anchor data for frontend

If no salary API exists, create one with anonymized market data.

### Step 3: Build salary comparator component
Enhance or create salary comparator UI:
- Visual bar showing market range (low → median → high)
- User's target salary marked on the bar with color indicator
- Green if above median, yellow if near median, red if significantly below
- "Ask for more" CTA that suggests negotiation talking points
- Comparison context: "You are X% below market median for this role"

### Step 4: Integrate with salary-query.tsx
- Show comparator after salary query results
- Make it actionable - links to negotiation tips or CV improvement

### Step 5: Update progress DB
Mark #15 in_progress when you start, completed when done.

## Keywords
anchor, negotiation, parity
ULW