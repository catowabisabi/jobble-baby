Implement Doctor Visit Summary Export — generate shareable summary (PDF or text) of care log for pediatrician appointments. Reuses tracking/schedule/growth/milestones data. Add export button to doctor-visit.tsx.

## Context
- Project: Jobble Baby (Expo/React Native, expo-router, AsyncStorage)
- doctor-visit.tsx already exists at JobbleBaby/app/(tabs)/doctor-visit.tsx (440 lines)
- It already loads: trackingEntries, growthEntries, scheduleEntries, milestones, babyProfile
- i18n files at JobbleBaby/app/i18n/en.json + zh.json

## Requirements
1. Add "Export Summary" button to doctor-visit.tsx header/hero section
2. On press: generate text summary containing:
   - Baby name + age (computed from birthDate)
   - Sleep: 30-day average hours/day, sleep window (typical bedtime → wake time)
   - Feeding: 7-day average feeds/day, most common method (breast/bottle/solid)
   - Diaper: 7-day average changes/day
   - Growth: latest height/weight + percentile if available
   - Milestones: count achieved by category
3. Use React Native Share API (Share.share from react-native) to share as text
4. Format cleanly with emojis: 🛏️ Sleep • 🍼 Feed • 📈 Growth • 🎉 Milestones
5. Add i18n keys for export button label and summary sections

## Implementation Approach
- In doctor-visit.tsx: add calculateSleepAverage(), calculateFeedingStats(), getLatestGrowth() helper functions (already partially exist — check the file)
- Export button: <TouchableOpacity onPress={handleExport}> with icon 📋
- handleExport: builds summary string from loaded state data, calls Share.share()
- Fallback: if data not loaded yet, show Alert "No data to export yet"

## Keywords for this task: pediatric triage, parental rhythm sync, data pension

## Files to modify
- JobbleBaby/app/(tabs)/doctor-visit.tsx — add export functionality (DO NOT replace existing content)
- JobbleBaby/app/i18n/en.json — add keys under doctorVisit namespace
- JobbleBaby/app/i18n/zh.json — add keys under doctorVisit namespace

## Design
- Export button: warm accent color (amber/orange), large tap target
- Summary preview before share (optional — just share directly is fine)
- Voice-free, concise, parent-friendly language

Begin implementation now. When done, report which files were created/modified with line counts.
ULW