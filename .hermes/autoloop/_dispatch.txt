fix: implement interview readiness dashboard frontend wiring

## Context
Backend already has /api/v1/interviews/readiness endpoint returning: total_sessions, average_score, readiness_level, recent_trend[], focus_areas[], sessions[]. Frontend interview-readiness.tsx fetches but painpoint #12 says it's NOT properly wired - no session history tracking, no score progression chart.

## Task

### Step 1: Audit interview-readiness.tsx
Read src/app/interview-readiness.tsx fully. Check:
- Does it display sessions history (list of past sessions)?
- Does it show score progression chart (visual trend)?
- Does it show focus areas recommendations?
- Does it handle readiness_level ("building", "ready", "expert", "no_data") with proper UI?

### Step 2: Fix dashboard display
The readiness endpoint returns sessions array. Display each session as a card showing:
- Date/completed_at
- Job type (or "General")
- Overall score with color coding (red < 5, yellow 5-7, green > 7)
- Top strength and improvement area from feedback

Add a simple score trend chart using View with flexDirection row to show score bars horizontally.

Show focus_areas as a list of suggested practice topics.

### Step 3: Verify API URL
Check the fetch URL is exactly: `${API_BASE_URL}/interviews/readiness` (the backend router prefix is /api/v1/interviews)

### Step 4: Check if user auth token is being sent
Interview readiness data is user-specific. Ensure the fetch includes Authorization header with Bearer token.

## Keywords
focal-point, skeleton, fusion
ULW