# Sisyphus Task — Cycle 261

## Context
Jobble Baby: 72 tabs, TSC 0 errors. Cycle 261 — Critical Period Development Dashboard (concept #40, idea #65).

## Task: Implement Critical Period Development Dashboard tab

Create a new tab file: app/(tabs)/critical-periods.tsx

## Requirements

### Core Features
1. Period Map: Visual timeline (0-12 months) with color-coded critical/sensitive period bands:
   - Attachment: 0-6 months (green)
   - Language: 0-12 months (blue)
   - Motor: 2-9 months (amber)
   - Visual: 0-4 months (purple)
   - Gut Microbiome: 0-6 months (teal)
   - Gut-Brain Axis: 0-12 months (pink)

2. Baby Current Position: Calculate from baby's birth date (AsyncStorage key: @jobble/baby_birthdate):
   - Show which periods are ACTIVE, IMMINENT (within 2 weeks), or PASSED
   - Visual timeline with marker at baby's current age

3. Period Detail Cards: Each period card shows:
   - Period name and age range
   - What it is and why it matters (2-3 sentences)
   - Top 3 activities to optimize this period
   - Signs baby is in this period
   - Red flags for developmental delay

4. Smart Activity Suggestions: Context-aware suggestions based on active periods

5. Progress Logging: Log activities done per period. Show coverage dashboard.

### Technical Requirements
- Use existing theme: COLORS from ../theme, useTheme hook, useLanguage hook
- AsyncStorage keys: @jobble/baby_birthdate, @jobble/period_activities
- Follow existing tab patterns (like milestones.tsx, growth.tsx)
- i18n: add keys to en.json and zh.json under criticalPeriods
- TSC 0 errors required
- Register in app/(tabs)/_layout.tsx

### Data Model
- @jobble/baby_birthdate: ISO date string
- @jobble/period_activities: JSON { [period_id]: [{ date, activity, notes }] }

## Process
1. Create app/(tabs)/critical-periods.tsx
2. Run npx tsc --noEmit after creation — 0 errors required
3. Add i18n keys to en.json + zh.json
4. Register in _layout.tsx
5. Do NOT commit

## Keywords
contrast_sensitivity, entropy-witness, digestive_maturity

ULW
