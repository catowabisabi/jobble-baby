Continue: Implement Sleep Debt Dashboard — parental sleep deficit visualization.

## Context
- Repo: /mnt/c/Users/enoma/Desktop/opencode-work/agent-works/jobble-baby
- Bundle ID: com.jobblebaby.app
- Project: Expo/React Native (JobbleBaby/ subdirectory)
- All 16 tabs functional ✅ — see store/SUBMISSION_READINESS.md for full status

## Task
Implement Sleep Debt Dashboard (new tab: sleep-debt.tsx):

1. **Weekly Parent Sleep Debt Visualization**
   - Load parent sleep hours from @jobble/shift_state (existing shift-handoff AsyncStorage key)
   - Also allow manual parent sleep hour entry: @jobble/parent_sleep_entries (date + hours)
   - Calculate cumulative sleep deficit: (7 × 8hr target) - actual hours per week
   - Display as: debt card with hours in red/amber/green, trend arrow vs last week

2. **Regression Correlation Overlay**
   - Overlay baby sleep regression windows (from @jobble/schedule_entries — detect regression by >30min drop in longest sleep stretch over 2 consecutive nights)
   - Show correlation: "Sleep debt + regression = high burnout risk" banner

3. **Shift Redistribution Alert**
   - When parent sleep debt > 3 nights AND baby in regression → suggest shift redistribution
   - Use existing shift-handoff infrastructure (@jobble/shift_state)
   - Show alert: "Redistribute shifts?" with "Swap tonight" / "Ask partner" buttons

4. **Tab Integration**
   - Create app/(tabs)/sleep-debt.tsx
   - Add Tabs.Screen name="sleep-debt" in app/(tabs)/_layout.tsx
   - Add i18n keys: tabs.sleepDebt in en.json + zh.json
   - Tab icon: MaterialCommunityIcons "sleep" or "alert-decagram"

5. **Badge**
   - Add "Parental Stamina" badge (badgeService): completed 1 week of tracked sleep after regression

## Implementation Notes
- Reuse existing shift-handoff AsyncStorage schema
- Dark theme (COLORS from ThemeContext)
- All data local, no account needed
- No external APIs

## Files to create/modify
- app/(tabs)/sleep-debt.tsx — new file
- app/(tabs)/_layout.tsx — add Tabs.Screen for sleep-debt
- app/i18n/en.json — add tabs.sleepDebt
- app/i18n/zh.json — add tabs.sleepDebt
- app/context/BadgeContext.tsx — add Parental Stamina badge (or badgeService.ts)

Report what you created and the line count of sleep-debt.tsx.
ULW
