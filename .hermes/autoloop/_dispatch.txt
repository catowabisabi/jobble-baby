fix: add premium gate to cv-detail.tsx + fix broken JSX in job-alerts.tsx

## Context
job-alerts.tsx was just updated with PremiumGate but has a JSX syntax issue - the PremiumGate opening tag wraps the component but the closing tag is inside the return statement, which breaks the component structure. Also, cv-detail.tsx has no premium gating at all.

## Task

### Step 1: Fix job-alerts.tsx JSX structure
The current structure is broken. Fix it so PremiumGate properly wraps the entire return:
```typescript
// BEFORE (broken - PremiumGate closing tag inside return)
return (
    <PremiumGate featureName="求職警報設定">
    <ThemedView style={styles.container}>
      ...
    </ThemedView>
);
    </PremiumGate>  // WRONG - this is outside return
```

Fix the structure by moving the PremiumGate open tag to wrap the ThemedView and fixing indentation. The component structure should be:
```typescript
return (
    <PremiumGate featureName="求職警報設定">
      <ThemedView style={styles.container}>
        ...
      </ThemedView>
    </PremiumGate>
);
```

Also fix the loading state to show PremiumGate consistently:
```typescript
if (isLoading) {
    return (
      <PremiumGate featureName="求職警報設定">
        <ThemedView style={styles.container}>
          <View style={styles.loadingContainer}>
            <ActivityIndicator size="large" color="#007AFF" />
            <ThemedText type="small" style={styles.loadingText}>
              載入設定中...
            </ThemedText>
          </View>
        </ThemedView>
      </PremiumGate>
    );
}
```

### Step 2: Add PremiumGate to cv-detail.tsx
cv-detail.tsx shows AI-generated CV optimization/suggestions - this is a premium feature. Add PremiumGate wrapping the main content:

Import at top:
```typescript
import PremiumGate from '@/components/premium-gate';
```

Wrap the content - the main scrollable content showing the CV score breakdown and optimization suggestions should be behind the gate. The PremiumGate should be placed inside the ScrollView content, wrapping the score display and suggestion sections. Something like:

```typescript
return (
    <ThemedView style={styles.container}>
      <ScrollView contentContainerStyle={styles.scrollContent}>
        <PremiumGate featureName="CV 評分與優化">
          {/* existing score bar and feedback content */}
        </PremiumGate>
      </ScrollView>
    </ThemedView>
);
```

Keep the header/score badge outside the gate but gate the detailed breakdown and suggestions.

### Step 3: Verify no other screens need gating
Check if any other app screen shows premium content without gating:
- salary-query.tsx - this is FREE tier (market salary data), do NOT add gate
- explore.tsx - this is FREE tier, do NOT add gate
- interview-readiness.tsx - dashboard is free tier per task spec, do NOT add gate
- profile.tsx - profile management is free, only specific premium features inside are gated

### Step 4: No DB update needed for this fix

## Keywords
translucent layer, borderline, evaporation
ULW