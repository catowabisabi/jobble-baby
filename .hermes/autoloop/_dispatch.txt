# Sisyphus Task — Cycle 261

## Context
Jobble Baby: 59 tabs, TSC 0 errors. Cycle 261 — Privacy policy row in Profile tab has no onPress handler.

## Task: Fix Profile tab privacy row — add onPress to open privacy policy

## Problem
In `app/(tabs)/profile.tsx` line ~514, the privacy SettingRow has no onPress handler:
```
<SettingRow icon="🔒" label={t('profile.privacy')} rowStyles={rowStyles} />
```
Users cannot view the privacy policy from within the app. Need to add onPress that opens `https://jobblebaby.com/privacy` (or the docs/privacy-policy.html page).

## Requirements
1. Find the privacy SettingRow in `app/(tabs)/profile.tsx`
2. Add `onPress={() => Linking.openURL('https://jobblebaby.com/privacy')}` 
3. Also update the i18n key `profile.privacy` if it doesn't already include a hint that it's tappable (optional)
4. Run `npx tsc --noEmit` — must pass with 0 errors
5. Do NOT commit

## Keywords
privacy_row_handler, Linking.openURL, settings UX

ULW