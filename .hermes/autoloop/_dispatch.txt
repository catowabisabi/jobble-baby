# Sisyphus Task — Cycle 219 (re-dispatch)

## Context
Jobble Baby: 45+ tabs, TSC 0 errors, i18n done. WCAG 2.1 AA complete (294 labels). App dev-complete. Pending: Baby Safety Audit Navigator (todo #86).

## Task: Implement Baby Safety Audit Navigator tab

Create app/(tabs)/safety-audit.tsx — guided home safety checklist.

### Features
1. **Sleep Environment Check**
   - Crib slat spacing <2.375 inches (no cant-needle test)
   - No crib bumpers, pillow, or soft bedding
   - Firm mattress, fitted sheet only
   - Baby alone on back (no co-sleeping unless planned)
   - Checkbox for each item + pass/fail toggle

2. **Car Seat Safety**
   - Rear-facing confirmation
   - Harness slot at or below shoulder level
   - Chest clip at armpit level
   - Car seat expiration date check (sticker location)
   - Photo capture option for each zone

3. **Babywearing Safety**
   - M-position hips (frog legs)
   - Airway visible (chin off chest)
   - No forward-facing before 6 months
   - No unsupervised car in carrier

4. **Bath Safety**
   - Max 2 inches water depth
   - Elbow test confirmation (water temp <100°F/38°C)
   - No ring toys that can trap fingers
   - Never leave unattended

5. **Feeding Safety**
   - No honey before 12 months
   - Upright positioning during feeds
   - Paced bottle feeding to prevent aspiration
   - Proper head elevation

6. **Supervision Zones**
   - Fall prevention: elevated surfaces, stairs
   - Stair gates installed top and bottom
   - No unattended baby on changing table
   - Hot surface check

### Data
- AsyncStorage key: @jobble/safety_audit_entries
- Schema: { zone: string, items: { label: string, passed: boolean, photo?: string }[], date: string, notes: string }
- Each audit = one entry array, newest first
- Exportable summary for pediatrician/landlord

### Files
- Create: app/(tabs)/safety-audit.tsx
- Add Tabs.Screen to app/(tabs)/_layout.tsx
- Add i18n keys to en.json + zh.json:
  - tabs.safetyAudit, safetyAudit.title, safetyAudit.zones.*, safetyAudit.passLabel, safetyAudit.failLabel, safetyAudit.exportReport, safetyAudit.addPhoto

### Rules
- Use existing tab pattern (see schedule.tsx as reference)
- TSC must pass: npx tsc --noEmit
- i18n: all user-facing strings via t()
- Accessibility: reuse WCAG labels from other tabs
- No functionality changes to existing tabs

### After
- npx tsc --noEmit must pass
- Commit: git add -A && git commit -m "feat(tab): implement Baby Safety Audit Navigator"
ULW
===
Baby Safety Audit Navigator
