verify: validate all TypeScript fixes, confirm npx tsc --noEmit passes

## Context
We just committed 11 files of TypeScript fixes across the codebase. Before proceeding, verify that all TypeScript errors are resolved by running npx tsc --noEmit and confirming zero src/ errors.

## Task

### Step 1: Run TypeScript check
Run: `cd /mnt/c/Users/enoma/Desktop/opencode-work/agent-works/jobble-baby && npx tsc --noEmit 2>&1 | grep "^src/"`
If any errors remain in src/, report them exactly.

### Step 2: Check that the following fixes are in place
- job-alerts.tsx: has keyboardView style in styles object
- cv-detail.tsx: has PremiumGate wrapping the main scrollable content
- salary-query.tsx: has Text import and uses rangeLowFill/rangeHighFill (not rangeFill)
- index.tsx: no more type="strong" usages (should be smallBold)
- interview.tsx, interview-readiness.tsx, subscription.tsx: no more type="largeTitle" (should be title)

### Step 3: Verify premium gates
Check these screens have PremiumGate:
- src/app/job-alerts.tsx
- src/app/cv-detail.tsx
The following should NOT have gates:
- src/app/salary-query.tsx (market data is FREE)
- src/app/explore.tsx (job listing is FREE)

## Keywords
audit, validation, typescript, premium-gate
ULW