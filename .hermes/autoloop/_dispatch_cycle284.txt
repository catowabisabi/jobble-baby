Sisyphus Task - Cycle 284

Context:
Jobble Baby: App PASSES pre-submission audit (TSC 0 errors, all REQUIRED checks passed).
The audit script correctly runs from JobbleBaby/ directory.
Remaining issue: 10 TouchableOpacity elements missing accessibilityLabel in 4 tab files.

Task: Fix Accessibility Labels - 4 Files (10 elements)

Add accessibilityLabel to each TouchableOpacity at the exact lines below.
Use existing i18n keys where available (check the t() calls in each file for appropriate keys),
or use descriptive English strings otherwise.

File 1: app/(tabs)/asymmetric-growth.tsx
- Line 247: <TouchableOpacity style={styles.saveButton} onPress={handleAdd}>
  → Add: accessibilityLabel={t('asymmetric.save')}

File 2: app/(tabs)/circadian.tsx
- Line 565: <TouchableOpacity style={styles.expandBtn} onPress={() => setShowLightLogger(!showLightLogger)}>
  → Add: accessibilityLabel={showLightLogger ? (t('duskLight.hideLogger') || 'Hide Logger') : (t('duskLight.logLight') || 'Log Light Exposure')}
- Line 590: <TouchableOpacity style={styles.logBtn} onPress={handleLogLight}>
  → Add: accessibilityLabel={t('duskLight.saveLog') || 'Save Light Log'}
- Line 603: <TouchableOpacity style={[styles.alarmBtn, ...]} onPress={() => handleSaveAlarm(...)}>
  → Add: accessibilityLabel={duskAlarm.enabled ? (t('duskLight.enabled') || 'Alarm is ON') : (t('duskLight.disabled') || 'Alarm is OFF')}
- Line 609: <TouchableOpacity style={[styles.alarmBtn, { flex: 1 }]} onPress={() => handleSaveAlarm(Math.max(0, duskAlarm.hour - 1), ...)}>
  → Add: accessibilityLabel={t('duskLight.decreaseHour') || 'Decrease alarm hour by 1'}
- Line 612: <TouchableOpacity style={[styles.alarmBtn, { flex: 1 }]} onPress={() => handleSaveAlarm(Math.min(23, duskAlarm.hour + 1), ...)}>
  → Add: accessibilityLabel={t('duskLight.increaseHour') || 'Increase alarm hour by 1'}
- Line 656: <TouchableOpacity style={styles.calcBtn} onPress={handleCalculatePhaseShift}>
  → Add: accessibilityLabel={t('duskLight.calculate') || 'Calculate Phase Shift'}

File 3: app/(tabs)/eight-month-storm.tsx
- Line 180: <TouchableOpacity style={[styles.modalBtn, { backgroundColor: '#666' }]} onPress={() => setShowAdd(false)}>
  → Add: accessibilityLabel={language === 'zh' ? '取消添加' : 'Cancel add entry'}
- Line 183: <TouchableOpacity style={[styles.modalBtn, { backgroundColor: C.accent }]} onPress={() => setShowAdd(false)}>
  → Add: accessibilityLabel={language === 'zh' ? '保存記錄' : 'Save entry'}

File 4: app/(tabs)/jaundice-threshold.tsx
- Line 297: <TouchableOpacity style={styles.saveButton} onPress={handleSave}>
  → Add: accessibilityLabel={t('jaundiceThreshold.save')}

Requirements:
- TSC must pass with 0 errors after changes (run: cd JobbleBaby && npx tsc --noEmit)
- No console.log/debug/error statements added
- Keep all existing functionality intact

Verification:
After fixing, run: cd JobbleBaby && node scripts/pre-submission-audit.js
All REQUIRED checks should pass. The hardcoded strings warning is non-blocking.

ULW - execute without asking