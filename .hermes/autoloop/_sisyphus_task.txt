# Sisyphus Task — Cycle 277

## Context
Jobble Baby: TSC 0 errors, 67 tabs. Three tabs have zero accessibilityLabel attributes on interactive elements.

## Task: Fix Accessibility on 3 Tabs

Add accessibilityLabel to all interactive elements in these 3 files:
- `JobbleBaby/app/(tabs)/allergens.tsx`
- `JobbleBaby/app/(tabs)/monitor-correlation.tsx`
- `JobbleBaby/app/(tabs)/projection.tsx`

## Requirements

### Per Tab
1. Find ALL TouchableOpacity, Pressable, TouchableWithoutFeedback components
2. Add `accessibilityLabel={t('some.key')}` — use existing i18n keys where possible; add new keys to `en.json` + `zh.json` if needed
3. For icon-only buttons: add meaningful accessibilityLabel
4. For images: add `accessibilityRole="image"` and alt text via accessibilityLabel
5. Do NOT change any logic, styles, or functionality — only add accessibilityLabel props

### i18n Keys (add if not exist)
- `allergens.tab_title` → "Allergens"
- `allergens.add_entry` → "Add allergen entry"
- `allergens.allergen_card` → "Allergen: {name}"
- `monitorCorrelation.tab_title` → "Monitor Correlation"
- `monitorCorrelation.add_entry` → "Add correlation entry"
- `projection.tab_title` → "Projection"
- `projection.add_entry` → "Add projection entry"

Add Chinese translations to `zh.json` for any new keys.

### Verification
- Run `cd JobbleBaby && npx tsc --noEmit` — must pass with 0 errors
- Do NOT commit — leave changes unstaged for Hermes review

### Keywords
accessibility_label,a11y,WCAG,interactive_element,touchable,accessibilityRole,screen_reader

ULW — execute without asking