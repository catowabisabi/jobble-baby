# Sisyphus Task — Cycle 285

## Context
Jobble Baby: Pre-submission audit PASSES all required checks (TSC 0 errors, 67 tabs). App is ready for App Store / Play Console submission pending user actions (EAS credentials, app creation, privacy policy URL).

The audit flagged NON-BLOCKING warnings: 22 hardcoded string arrays in UI components. These need i18n keys.

## Task: Fix Non-i18n UI Strings in 3 Tabs

The pre-submission-audit.js flags hardcoded string arrays in several files. Fix these three files:

### File 1: app/(tabs)/bonding-journal.tsx
Lines 59-60: Rating labels ('Struggling', 'Low', 'Neutral') and color hex values are hardcoded.
- Add i18n keys for the rating labels (e.g., bonding.journal.rating.struggling, bonding.journal.rating.low, bonding.journal.rating.neutral)
- Add keys to both en.json and zh.json
- Color hex values (data array) can stay hardcoded as they are not UI-displayed strings

### File 2: app/(tabs)/colic-relief.tsx
Lines 16-17: Cause categories ('hunger', 'wind', 'overtired') and sound types ('vacuum', 'fan', 'shusher') are hardcoded.
- Add i18n keys: colic.cause.hunger, colic.cause.wind, colic.cause.overtired, etc.
- Add keys to both en.json and zh.json

### File 3: app/(tabs)/cry-analyzer.tsx
Line 74: Weekday labels ('Sun', 'Mon', 'Tue'...) are hardcoded.
- Use the existing day name pattern or add i18n keys for cry.analyzer.day.sun through cry.analyzer.day.sat
- Or better: replace hardcoded array with useMemo deriving from Intl.DateTimeFormat

## Requirements
- TSC must pass with 0 errors after changes
- No console.log/debug/error statements
- Keep existing functionality intact
- Add keys to BOTH en.json AND zh.json in app/i18n/

## Verification
After fixing, run: cd JobbleBaby && node scripts/pre-submission-audit.js
The Hardcoded Strings check should show fewer warnings (color arrays and numeric data are expected to remain).

## Keywords
stress_response,clogged_milk_gland,body_awareness

ULW — execute without asking