# Sisyphus Task — Cycle 315: Tab Navigator Integrity Check

## Context
Jobble Baby has 68 tabs. We need to verify ALL tab .tsx files are registered in _layout.tsx Tabs.Screen — no orphaned screens.

## Your Task
1. Read `app/(tabs)/_layout.tsx` — extract ALL Tabs.Screen names
2. List ALL .tsx files in `app/(tabs)/`
3. Compare: which .tsx files are NOT in Tabs.Screen? List them.
4. For each unregistered file: check if it has a valid component (not empty stub)
5. If any unregistered files have real content → add them to _layout.tsx Tabs.Screen

## Verification
- After any fixes: run `npx tsc --noEmit` — must pass 0 errors
- Report: registered count, unregistered count, orphaned files

## Output Format
```
=== Tab Navigator Integrity ===
Registered: N/68
Unregistered: [list]
Fixed: [list or "none needed"]
TSC: PASS/FAIL
```

Start in: /mnt/c/Users/enoma/Desktop/opencode-work/agent-works/jobble-baby/JobbleBaby
ULW