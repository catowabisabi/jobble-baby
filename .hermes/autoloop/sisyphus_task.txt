# Fix react-native-svg Version Mismatch

## Goal
Fix the expo-doctor warning: react-native-svg version mismatch (expected 15.15.4, found 15.15.5).

## Steps
1. Run: `npx expo install --check` in JobbleBaby/ to review dependencies
2. Run: `npx expo install react-native-svg` to align to correct version
3. Run: `npx tsc --noEmit` — must pass 0 errors
4. Verify expo-doctor shows 21/21 passing

## Verification
- `npx expo-doctor` must show 21/21 checks passed (0 failed)
- TSC 0 errors
- Commit + push

ULW
