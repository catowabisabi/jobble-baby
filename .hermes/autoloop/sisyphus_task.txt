# Task #489 — Complete Cry + Feeding Readiness i18n + Storage Keys

## Context
Hermes fixed 2 TSC errors in feeding-readiness-navigator.tsx (line 323: icon name, line 428: style type).
cry-acoustic-fingerprint.tsx was created but uses hardcoded English strings (no i18n).
Both tabs now compile (TSC: 0 errors).

## Your Job — Complete the following:

### STEP 1 — Add CRY storage keys to storage-keys.ts
Read `JobbleBaby/store/storage-keys.ts` first.
Add these two entries to the STORAGE_KEYS object (follow the existing format):
- `CRY_EVENTS: "@jobble/cry_events"`
- `CRY_CORRELATIONS: "@jobble/cry_correlations"`

### STEP 2 — Add i18n keys to cry-acoustic-fingerprint.tsx
Read `JobbleBaby/app/(tabs)/cry-acoustic-fingerprint.tsx` to find ALL hardcoded English strings.
Read `JobbleBaby/app/i18n/en.json` and `zh.json` to understand the format.
Add i18n keys under `"cryAcoustic"` in both en.json and zh.json for every hardcoded string in the component.

Key strings to internationalize include (but are not limited to):
- Section titles: "Cry Fingerprint", "Cry Events", "Correlation Dashboard", etc.
- Labels: "Cry Type", "Duration (min)", "Trigger", "Response", "Confidence", etc.
- Button text: "Add Entry", "Recording...", "Stop", "Save"
- Empty state: "No cry events recorded yet"
- Chart labels: day labels, frequency labels

Keep the tab functional (mock data is OK), just make it bilingual.

### STEP 3 — Verify feeding-readiness i18n completeness
Read `JobbleBaby/app/(tabs)/feeding-readiness-navigator.tsx` and check all `t('...')` calls.
Compare with `en.json` and `zh.json` feedingReadinessMultisensor keys.
Add any missing i18n keys.

### STEP 4 — Register CRY storage keys in cry-acoustic-fingerprint.tsx
Update the component to import STORAGE_KEYS from `../store/storage-keys` (or `../../store/storage-keys` depending on path) and use STORAGE_KEYS.CRY_EVENTS and STORAGE_KEYS.CRY_CORRELATIONS instead of the local string constants.

### STEP 5 — Verify
- Run `npx tsc --noEmit` → 0 errors
- Git add + commit with message: "chore: complete i18n for cry-acoustic-fingerprint and feeding-readiness, add CRY storage keys"

Do NOT run npm install or modify package.json.

ULW
