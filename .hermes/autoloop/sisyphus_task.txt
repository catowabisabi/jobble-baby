# Task: Fix accessibility on bottle-feeding.tsx (Todo #119)

## Overview
The bottle-feeding.tsx tab has 9 TouchableOpacity elements with zero accessibilityLabel. All other tabs have been WCAG 2.1 AA compliant, but the newest tab (bottle-feeding) was missed. Fix all interactive elements.

## Files
- app/(tabs)/bottle-feeding.tsx (557 lines, 9 touchable elements without accessibilityLabel)

## What to Fix
Add `accessibilityLabel` prop to ALL of these TouchableOpacity elements:
1. Nipple level selector item (line ~183)
2. Each nipple level option in modal (line ~241, ~265)
3. "Log Session" add button (line ~281)
4. "Log Practice" add button (line ~335)
5. Each pace step item (line ~383)
6. Modal close buttons (lines ~393, ~461, ~489)
7. Any other TouchableOpacity/Pressable without label

## Rules
- Use existing i18n keys where possible (e.g., t('bottleFeeding.nippleLevel0'))
- For buttons without existing keys, add new keys to en.json + zh.json
- Keep existing onPress handlers intact
- TSC must pass with 0 errors after changes

## Verification
1. npx tsc --noEmit → 0 errors
2. Count accessibilityLabel occurrences in file → should be 15+
3. All TouchableOpacity/Pressable elements have labels

## Keywords
bottleNippleFlow, pacedBottleFeeding, feedingEfficiency, slowFlowNipple, fastFlowTransition
ULW