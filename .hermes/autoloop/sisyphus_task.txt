#!/bin/bash
# Sisyphus Task — Cycle 223
# Task: Fix Sentry install with --legacy-peer-deps + configure

set -e
cd /mnt/c/Users/enoma/Desktop/opencode-work/agent-works/jobble-baby/JobbleBaby

echo "=== Cycle 223: Sentry Fix ==="

# Step 1: Install @sentry/react-native with --legacy-peer-deps
echo "Installing @sentry/react-native..."
npm install @sentry/react-native --legacy-peer-deps
echo "Done"

# Step 2: Create docs/SENTRY_SETUP.md
cat > /mnt/c/Users/enoma/Desktop/opencode-work/agent-works/jobble-baby/docs/SENTRY_SETUP.md << 'MDEOF'
# Sentry Crash Reporting Setup

## Overview
@sentry/react-native provides crash reporting and performance monitoring for Jobble Baby.

## Installation
`npm install @sentry/react-native --legacy-peer-deps`

## Configuration

### 1. App.tsx
Import and initialize Sentry at app entry point:
```typescript
import * as Sentry from '@sentry/react-native';

Sentry.init({
  dsn: process.env.EXPO_PUBLIC_SENTRY_DSN,
  environment: __DEV__ ? 'development' : 'production',
  enableAutoPerformanceTracing: true,
  tracesSampleRate: __DEV__ ? 1.0 : 0.1,
  reactNavigationV5Instrumentation: true,
});
```

Wrap App with Sentry.ReactNativeProfiler:
```typescript
export default Sentry.NativeProfiler(App);
```

### 2. Environment Variables
Add to eas.json or .env:
- `SENTRY_DSN`: DSN from sentry.io project settings

### 3. EAS Build Sourcemaps
Add to eas-build.yml:
```yaml
- name: Build iOS
  steps:
    - run: |
        eas build:inspect --platform ios --output ./build-results
      env:
        SENTRY_AUTH_TOKEN: ${{ secrets.SENTRY_AUTH_TOKEN }}
    - run: npx sentry-cli releases propose-sourcemaps
```

## GitHub Secrets Required
- `SENTRY_AUTH_TOKEN`: Auth token from sentry.io (settings → auth tokens)
- `SENTRY_ORG`: Organization slug
- `SENTRY_PROJECT`: Project name (com.jobblebaby.app)

## Verification
After setup, test by triggering a JS error — it should appear in Sentry dashboard within ~30 seconds for production builds.
MDEOF

# Step 3: Update App.tsx to add Sentry
echo "Checking App.tsx..."
APP_TSX="/mnt/c/Users/enoma/Desktop/opencode-work/agent-works/jobble-baby/JobbleBaby/app/App.tsx"

if grep -q "Sentry" "$APP_TSX" 2>/dev/null; then
    echo "Sentry already configured in App.tsx"
else
    echo "Adding Sentry to App.tsx..."
    # Add Sentry import after React import
    sed -i "s/import \* as React from 'react';/import * as React from 'react';\nimport * as Sentry from '@sentry\/react-native';/" "$APP_TSX"
    
    # Add Sentry.init before App export
    sed -i "s/export default function App/if (!__DEV__) {\n  Sentry.init({\n    dsn: process.env.EXPO_PUBLIC_SENTRY_DSN,\n    environment: 'production',\n    enableAutoPerformanceTracing: true,\n    tracesSampleRate: 0.1,\n    reactNavigationV5Instrumentation: true,\n  });\n}\n\nexport default function App/" "$APP_TSX"
    
    echo "Sentry added to App.tsx"
fi

# Step 4: Verify TypeScript compiles
echo "Running TypeScript check..."
cd /mnt/c/Users/enoma/Desktop/opencode-work/agent-works/jobble-baby/JobbleBaby
npx tsc --noEmit
echo "TSC passed"

# Step 5: Commit
cd /mnt/c/Users/enoma/Desktop/opencode-work/agent-works/jobble-baby
git add -A
git commit -m "feat(ops): add @sentry/react-native crash reporting + docs/SENTRY_SETUP.md

- Install @sentry/react-native (--legacy-peer-deps)
- Add Sentry.init() to App.tsx with EXPO_PUBLIC_SENTRY_DSN
- docs/SENTRY_SETUP.md: setup guide + GitHub secrets + sourcemap config
- TSC 0 errors"

echo "=== Cycle 223 complete ==="
echo "Commit: $(git log -1 --oneline)"
exit 0