# Task: Implement Jaundice Tracker — Idea #57

## Context
You are implementing Idea #57 from the progress.db (Jobble Baby project, Expo SDK 56, expo-router tabs).
All data stored in AsyncStorage under @jobble/* keys. TSC must pass 0 errors before commit.

## Your Task
Implement a new tab: app/(tabs)/jaundice.tsx

### Features Required

1. **Jaundice Home Dashboard**
   - Baby info header (name, birth date, age in days/weeks) — reuse profile.tsx data
   - Current risk level indicator: Low / Moderate / High (based on bilirubin if entered)
   - "Log Reading" FAB button
   - Recent entries list (date, bilirubin mg/dL, method: blood test / transcutaneous)
   - Alert banner if bilirubin >12 mg/dL for breastfed or >15 mg/dL for formula-fed

2. **Bilirubin Log Entry**
   - Date picker (default: today)
   - Bilirubin value input (mg/dL, numeric with 1 decimal)
   - Measurement method toggle: Blood test / Transcutaneous (Tc) meter
   - Baby age in days (auto-calculated from birth date in profile)
   - Notes field (optional)
   - Save to AsyncStorage @jobble/jaundice_entries

3. **Phototherapy Tracker**
   - "Start Phototherapy Session" button
   - Start/end time pickers
   - Lamp type selector: LED / Halogen / Fiber Optic / BiliBlanket
   - Duration auto-calculated
   - Session history list
   - Save to AsyncStorage @jobble/phototherapy_entries

4. **Feeding Correlation**
   - Show feeding frequency from @jobble/tracking_entries (last 7 days avg)
   - "Low feeding = dehydration risk" alert if avg <6 feeds/day
   - Link to tracking tab

5. **Risk Chart**
   - WHO/NICE age-adjusted bilirubin risk chart (text-based threshold table)
   - Age in days (rows) vs Risk level (Low/Medium/High/Exchange)
   - Highlight current reading on chart if entered

6. **Data Export**
   - "Share Report" button → expo-sharing with text summary
   - Include: baby name, age, all bilirubin readings, phototherapy sessions, feeding avg

### Data Model (AsyncStorage)
- @jobble/jaundice_entries: [{ id, date, bilirubin, method, ageDays, notes }]
- @jobble/phototherapy_entries: [{ id, date, startTime, endTime, lampType, durationMin }]

### Integration Points
- Reuse profile.tsx for baby name, birth date (@jobble_baby_profile)
- Reuse tracking.tsx for feeding data (@jobble/tracking_entries)
- i18n: add keys to en.json + zh.json (tabs.jaundice, jaundice.*, phototherapy.*, risk.*)

### Files to Create/Modify
- app/(tabs)/jaundice.tsx — NEW (~300-350 lines)
- app/(tabs)/_layout.tsx — register jaundice tab (icon: MaterialCommunityIcons name="lightbulb-outline" or "medical-bag", label: t('tabs.jaundice'))
- app/i18n/en.json — add all t('tabs.jaundice'), t('jaundice.*'), t('phototherapy.*'), t('risk.*') keys
- app/i18n/zh.json — add all Chinese translations

### Verification
- `npx tsc --noEmit` must pass 0 errors
- Tab visible and tappable in tab bar
- Bilirubin entry saves and displays in list
- Alert banner shows when reading exceeds threshold
- Run in Expo Go or prebuild to verify UI

## Commit message
feat(tab): implement Jaundice Tracker — bilirubin log, phototherapy sessions, risk chart, feeding correlation (idea #57)

## Keywords
delight, jaundice, phototherapy

ULW
