# Pediatric Report PDF Export — Task for Sisyphus

## Goal
Implement **Pediatric Report PDF Export** tab — generate a shareable PDF summary of the baby's health data for pediatrician visits, including growth chart with WHO percentile bands, milestone timeline, feeding history, and sleep summary.

## Tech Stack
- Expo SDK (managed workflow) — NO react-native-pdf-lib or react-native-html-to-pdf (not compatible with Expo managed workflow)
- Use **expo-print** + **expo-sharing** for PDF generation and sharing
- Build HTML string → `await Print.printToFileAsync({ html })` → share via `Sharing.shareAsync(uri)`

## Reference Files
- `app/(tabs)/growth.tsx` — WHO growth data structure and chart data (uses @jobble/growth_entries)
- `app/(tabs)/milestones.tsx` — milestone timeline (uses @jobble/milestone_photos)
- `app/(tabs)/tracking.tsx` — feeding/diaper/sleep log (uses @jobble/tracking_entries)
- `app/(tabs)/allergens.tsx` — allergen status (uses @jobble/allergen_entries)
- `@jobble_baby_profile` — baby name, birth date, gender

## Files to Create/Modify
1. Create: `app/(tabs)/pediatric-report.tsx`
2. Modify: `app/(tabs)/_layout.tsx` — add Tabs.Screen entry
3. Modify: `app/i18n/en.json` — add i18n keys
4. Modify: `app/i18n/zh.json` — add i18n keys
5. Modify: `app/data/badges.ts` — add badge

## UI Layout
1. **Header**: "Pediatric Report" (en) / "兒科報告" (zh)
2. **Baby Info Card**: Name, birth date, current age (calculated from profile), gender
3. **Report Preview** (scrollable):
   - Growth Chart: text table showing date, weight (kg), height (cm), head circumference, with WHO percentile ranges labeled
   - Milestone Timeline: chronological list of key milestones with dates and ages
   - Feeding Summary: last 7 days aggregated (total feeds, total ml/breastfeeding duration)
   - Sleep Summary: last 7 days aggregated (total sleep hours, average longest stretch)
   - Allergen Status: quick list of introduced allergens and dates
4. **Export Button**: "Export PDF" → `Print.printToFileAsync({ html })` → `Sharing.shareAsync(uri)`

## HTML Template for PDF
Build a clean HTML string with inline CSS. Style for A4 printing. Use system fonts. Structure:
```
<!DOCTYPE html>
<html><head><meta charset="utf-8"><style>
  body { font-family: -apple-system, sans-serif; padding: 20px; color: #333; }
  h1 { color: #1a1a2e; border-bottom: 2px solid #3B82F6; }
  h2 { color: #3B82F6; margin-top: 20px; }
  table { width: 100%; border-collapse: collapse; margin-top: 10px; }
  th, td { border: 1px solid #ddd; padding: 8px; text-align: left; }
  th { background: #f0f4ff; }
  .section { page-break-inside: avoid; }
</style></head><body>
<h1>${babyName} — Pediatric Health Report</h1>
<p>Generated: ${date} | Baby age: ${age}</p>
... sections ...
</body></html>
```

## i18n Keys to Add
tabs.pediatricReport, pediatricReport.title, pediatricReport.exportPdf, pediatricReport.generated, pediatricReport.babyName, pediatricReport.ageMos, pediatricReport.growthSection, pediatricReport.milestoneSection, pediatricReport.feedingSection, pediatricReport.sleepSection, pediatricReport.allergenSection, pediatricReport.noData, pediatricReport.weight, pediatricReport.height, pediatricReport.headCirc, pediatricReport.percentile, pediatricReport.date, pediatricReport.milestone, pediatricReport.ageAtMilestone

## Tab Registration
name="pediatric-report", icon="file-document-outline", label=t("tabs.pediatricReport")

## Badge to Add
id: pediatric_report, name: "Doctor Ready", description: "Exported a pediatric report", icon: "📋", category: "milestone"

## Data Reading
All data read from existing AsyncStorage keys — no new storage schema needed. Parse existing entries, aggregate for PDF.

## VERIFICATION
npx tsc --noEmit in JobbleBaby/ must pass 0 errors before reporting done. ULW