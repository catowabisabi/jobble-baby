# Sisyphus Task — Cycle 267

## Context
Jobble Baby: 61 tabs, TSC 0 errors. Cycle 267 — App Store + Play Store Screenshot Automation (idea #66, todo #107)

## Task: Generate App Store + Play Store Screenshots for All 61 Tabs

Create an automated screenshot generation system for all 61 tabs at correct store dimensions.

## Requirements

### Dimensions
- **App Store (iPhone 6.7")**: 1290×2796 px, PNG
- **Play Store (phone)**: 1080×2340 px (or 1024×500 feature graphic), PNG

### Process
1. **Install dependencies**:
   ```bash
   cd /mnt/c/Users/enoma/Desktop/opencode-work/agent-works/jobble-baby/JobbleBaby
   npm install --legacy-peer-deps react-native-view-shot
   ```
   OR use `npx expo-screenshots` if available.

2. **Create screenshot generation script**: `scripts/generate-screenshots.ts`
   - Use `react-native-view-shot` to capture each tab screen
   - Loop through all 61 tab files in `app/(tabs)/`
   - Output App Store screenshots to `assets/screenshots/app-store/`
   - Output Play Store screenshots to `assets/screenshots/play-store/`
   - Use consistent framing: show the tab header/title and main content area

3. **Run the script** to generate all screenshots

4. **Fallback**: If automated capture is not feasible, create a **placeholder screenshot template** for each tab at correct dimensions using a simple colored rectangle with tab name text overlay. Save to the output directories.

5. **Verify** all 61 tabs have screenshots in both directories

### Tab List (61 tabs)
```
allergens, appstore-checklist, bilateral-coordination, bonding-journal, bottle-refusal,
caregiver-fatigue, circadian, clinician-portal, colic-relief, constellation,
critical-periods, cry-analyzer, doctor-visit, eight-month-storm, feeding-readiness,
fontanelle-hydration, fontanelle, gear-check, growth-montage, growth,
gut-brain-axis, habit-reset, hip-click, index, iot-security,
jaundice, jet-lag, launch-checklist, medicine-dose, milestones,
milk-prep, milk-transfer, monitor-correlation, moro-reflex, oral-motor,
pediatric-report, phototherapy-comfort, procedure-recovery, products, profile,
projection, reflex-integration, reflex-tracker, regression-navigator, safety-audit,
schedule, sensory-integration, shift-handoff, sleep-architecture, sleep-association,
sleep-debt, sleep-training, stress-cascade, teething, thermal-regulation,
tongue-tie, tracking, tummy-time, vestibular-assessment, weaning-rash
```

### Output Structure
```
assets/screenshots/
  app-store/
    allergens.png
    appstore-checklist.png
    ... (all 61)
  play-store/
    allergens.png
    appstore-checklist.png
    ... (all 61)
```

### Technical Requirements
- If using react-native-view-shot: `import { captureScreen } from 'react-native-view-shot'`
- Script should be runnable with: `npx ts-node scripts/generate-screenshots.ts` or `npx tsx scripts/generate-screenshots.ts`
- TSC 0 errors for any new code
- Do NOT commit screenshots to git (add to .gitignore pattern: `assets/screenshots/**`)
- Do NOT modify existing tab code

## Keywords
app_store_screenshot, play_store_screenshot, automated_screenshot, view_shot, expo_screenshots, app_store_listing, play_store_listing, placeholder_screenshot, screenshot_generation, store_assets
ULW