# TASK #377 — Fix hardcoded label arrays → i18n (5 tab files)

Repo: /mnt/c/Users/enoma/Desktop/opencode-work/agent-works/jobble-baby/JobbleBaby

## Task

Fix hardcoded English label arrays in 5 tab files. Replace with i18n `t()` calls. Add keys to en.json + zh.json.

## Files to Fix

### 1. `app/(tabs)/bilateral-coordination.tsx`
Hardcoded: `['Grasping', 'Kicking', 'Reaching', 'Crawling', 'Waving', 'Clapping', 'Left', 'Right', 'Both/Neither']` (10 items)
- Find the array
- Add `ti('bilateral-coordination.label.<key>')` keys to en.json + zh.json
- Replace with `t('bilateral-coordination.label.<key>')` calls
- TSC 0 errors

### 2. `app/(tabs)/circadian.tsx`
Hardcoded: phase labels `['Sleep', 'Feeding', 'Wake']` + fallback/default state labels
- Find the array
- Add i18n keys to en.json + zh.json
- Replace with `t('...')` calls
- TSC 0 errors

### 3. `app/(tabs)/colic-relief.tsx`
Hardcoded: `['Bicycle Legs', 'Belly Massage', 'Warm Compress', 'Gripe Water', 'Probiotic Drops']` (5 items)
- Find the array
- Add i18n keys to en.json + zh.json
- Replace with `t('...')` calls
- TSC 0 errors

### 4. `app/(tabs)/caregiver-fatigue.tsx`
Hardcoded: `['Feeding', 'Sleep', 'Diaper', 'Medicine', 'Appointments', 'Playtime', 'Bathing', 'Travel']` (8 items)
- Find the array
- Add i18n keys to en.json + zh.json
- Replace with `t('...')` calls
- TSC 0 errors

### 5. `app/(tabs)/asymmetric-growth.tsx`
Hardcoded: `['Normal', 'Mild', 'Moderate', 'Significant']` (4 items)
- Find the array
- Add i18n keys to en.json + zh.json
- Replace with `t('...')` calls
- TSC 0 errors

## Verification
1. `npx tsc --noEmit` → 0 errors
2. All user-facing labels now use i18n `t()` calls
3. No hardcoded English strings remain in these 5 files

## Pattern
Look at existing `ti()` or `t()` picker/array patterns in the codebase. Use same naming convention.

Keywords: colic_diurnal_variation_index, island_rule, witch hazel_safe_dilution
ULW
