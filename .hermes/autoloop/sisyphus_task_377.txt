FIX: regulatory-fitness.tsx CASCADE_ALERTS hardcoded English messages (painpoint #26)

Repo: /mnt/c/Users/enoma/Desktop/opencode-work/agent-works/jobble-baby/JobbleBaby

## Task

The `CASCADE_ALERTS` array in `app/(tabs)/regulatory-fitness.tsx` (lines 27-30) has hardcoded English user-facing messages:

```
const CASCADE_ALERTS = [
  { domain: 'sensory', threshold: 30, message: 'Low sensory score → may affect sleep in 2-3 days' },
  { domain: 'autonomic', threshold: 30, message: 'Low autonomic score → may increase crying' },
  { domain: 'motor', threshold: 30, message: 'Low motor score → may affect exploration' },
  { domain: 'social', threshold: 30, message: 'Low social score → may affect bonding' },
];
```

The `message` field is rendered directly in the UI (in `CascadeAlertBanner`). These need i18n.

## Steps

1. Read `app/(tabs)/regulatory-fitness.tsx` — find the CASCADE_ALERTS array and understand how `message` is used in JSX
2. Read `app/i18n/en.json` — find `regulatoryFitness` section (grep "regulatoryFitness")
3. Read `app/i18n/zh.json` — find same section
4. Add i18n keys under `regulatoryFitness.cascadeAlert.*` for each domain message in both en.json and zh.json
5. Refactor CASCADE_ALERTS to use `messageKey` instead of `message` (e.g., `messageKey: 'regulatoryFitness.cascadeAlert.sensory'`)
6. Update the JSX that renders `alert.message` to use `t(alert.messageKey)` instead
7. Run `cd /mnt/c/Users/enoma/Desktop/opencode-work/agent-works/jobble-baby/JobbleBaby && npx tsc --noEmit` — must pass with 0 errors
8. Run `cd /mnt/c/Users/enoma/Desktop/opencode-work/agent-works/jobble-baby/JobbleBaby && node scripts/pre-submission-audit.js` — must PASS

## Rules
- Only touch files related to this fix
- Do NOT change the threshold values or domain names
- The `message` field in CASCADE_ALERTS objects should be replaced with `messageKey`
- Use the `t()` hook from `useLanguage()` just like the rest of the file

**DONE ULW**
