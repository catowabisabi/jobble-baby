Repo: /mnt/c/Users/enoma/Desktop/opencode-work/agent-works/jobble-baby/JobbleBaby

## Task

Fix hardcoded user-visible string labels in `app/(tabs)/constellation.tsx` → convert to i18n using `t()` helper.

### File: constellation.tsx

This file has 19 hardcoded labels in `MILESTONE_CLUSTERS` array (module-level, lines 8-46).

The `MILESTONE_CLUSTERS` array structure:
```tsx
const MILESTONE_CLUSTERS = [
  {
    name: 'Motor Orion',      // ← hardcoded cluster name (used on line 85: {c.name})
    color: '#3B82F6',
    milestones: [
      { id: 'head_control', label: 'Head Control', age: '0-2mo', connections: ['rolling', 'sitting'] },
      // ... 6 more milestones in this cluster
    ],
  },
  // 2 more clusters...
];
```

**Move `MILESTONE_CLUSTERS` inside the component** (after `const { t } = useLanguage()` on line 51) so `t()` is in scope.
Then replace each hardcoded `label:` with `t('constellation.milestone.<id>')` and each `name:` with `t('constellation.cluster.<key>')`.

**i18n keys to add to both `app/i18n/en.json` and `app/i18n/zh.json`:**

```json
"constellation": {
  "cluster": {
    "motorOrion": "Motor Orion",
    "socialCassiopeia": "Social Cassiopeia",
    "sensoryUrsa": "Sensory Ursa"
  },
  "milestone": {
    "headControl": "Head Control",
    "rolling": "Rolling Both Ways",
    "sitting": "Sitting Unsupported",
    "crawling": "Crawling",
    "pullingUp": "Pulling to Stand",
    "cruising": "Cruising",
    "walking": "First Steps",
    "socialSmile": "Social Smile",
    "eyeContact": "Eye Contact",
    "jointAttention": "Joint Attention",
    "strangerWariness": "Stranger Wariness",
    "separationAnxiety": "Separation Anxiety",
    "pointing": "Pointing",
    "gestures": "Gestures (bye-bye)",
    "words": "First Words",
    "vestibularTolerance": "Vestibular Tolerance",
    "balance": "Balance",
    "pincerGrasp": "Pincer Grasp",
    "selfFeeding": "Self Feeding"
  }
}
```

For zh.json, use Traditional Chinese (e.g., "Head Control" → "抬頭控制", "Rolling Both Ways" → "翻身", etc. — use your best judgment for each term).

**Pattern for replacing milestone labels:**
```tsx
// Before
{ id: 'head_control', label: 'Head Control', age: '0-2mo', connections: ['rolling', 'sitting'] }
// After
{ id: 'head_control', label: t('constellation.milestone.headControl'), age: '0-2mo', connections: ['rolling', 'sitting'] }
```

**Pattern for cluster names:**
```tsx
// Before
name: 'Motor Orion',
// After
name: t('constellation.cluster.motorOrion'),
```

**Note:** The `age:` field (e.g., '0-2mo') is NOT user-visible in the rendered UI — it is used as a data value. Do NOT i18n the age strings.

**Verification:**
After changes, run from `JobbleBaby/`:
```
npx tsc --noEmit
```
Must show 0 errors. Fix any TypeScript errors before completing.

**DONE ULW**
