# Task #490 — Fix Hardcoded Strings in Tab Files (P2 Painpoints #10, #25, #26)

## Context
Pre-submission audit PASSES but several P1/P2 painpoints remain about hardcoded English strings in tab files.
The hardcoded string audit (check 9/9) passed because it only catches strings directly in JSX <Text> elements
that skip the i18n `t()` hook. However, several tab files have hardcoded string ARRAYS (used for Options arrays, etc.)
that ARE technically accessible to the i18n system but still represent maintenance debt.

Priority order: fix the P2 painpoints (#10, #25, #26) — these are about string arrays in:
- interoceptive.tsx (26+ hardcoded strings in 4 arrays)
- phototherapy-comfort.tsx
- reflex-visual-motor.tsx
- and 5 other tab files per painpoint #26

## Your Job

### STEP 1 — Audit
Run this to find hardcoded string arrays in the tab files:
```
cd /mnt/c/Users/enoma/Desktop/opencode-work/agent-works/jobble-baby/JobbleBaby
grep -n "const [A-Z].*OPTIONS\|const [A-Z].*ITEMS\|const [A-Z].*TYPES\|const [A-Z].*REGIONS\|const [A-Z].*CATEGORIES" app/(tabs)/*.tsx | head -40
```
Check each match — if the array contains user-visible English strings (not i18n keys), it's hardcoded.

### STEP 2 — Fix interoceptive.tsx (P2 painpoint #25)
In `app/(tabs)/interoceptive.tsx`:
- Lines ~81-95: NEED_OPTIONS array (strings like 'Hunger', 'Thirst', etc.)
- Lines ~96-109: BODY_REGIONS array
- Lines ~110-130: SENSORY_QUALITIES array  
- Lines ~131-145: INTENSITY_LEVELS array
Move each array to a `const` at the top of the file where the values are defined inline in the array itself.
Use template: `const XXX_OPTIONS = ['key1', 'key2', ...]` referencing i18n keys.

### STEP 3 — Add i18n keys
For any NEW keys added to i18n (not already in en.json/zh.json), add them:
```
# Add to en.json under appropriate section:
"interoceptive": {
  "need": { "hunger": "Hunger", "thirst": "Thirst", ... },
  "bodyRegion": { "head": "Head", "face": "Face", ... },
  ...
}
# Add same structure to zh.json with Chinese translations
```

### STEP 4 — Fix other P2 tab files
Based on painpoint #26, also fix these files:
- bila.tsx (if it has hardcoded strings)
- Any other tab files with similar hardcoded string arrays

### STEP 5 — TSC Verification
```
cd /mnt/c/Users/enoma/Desktop/opencode-work/agent-works/jobble-baby/JobbleBaby
npx tsc --noEmit
```
Must pass 0 errors.

### STEP 6 — Pre-submission Audit
```
node scripts/pre-submission-audit.js
```
All checks must PASS.

## Keywords
hardcoded_string_cleanup, i18n_key_injection, interoceptive_arrays, body_regions_internationalization

## Output
Write a work log to .hermes/autoloop/sisyphus_response_490.txt summarizing:
- Files modified
- i18n keys added
- TSC result
- Audit result
