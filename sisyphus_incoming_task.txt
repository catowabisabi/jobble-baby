# Sisyphus Task — Cycle 457
# Priority: P3
# Project: Jobble Baby

## TASK: i18n Gap Fill — Chinese Translation Completion

**Keywords:** trunk_extension_symmetry · hydrogel_stiffness · rab3a_gtpase_cycle

### Objective
Fill the missing Chinese (zh) translation keys in zh.json to match en.json.

### Steps
1. cd /mnt/c/Users/enoma/Desktop/opencode-work/agent-works/jobble-baby
2. Run this to find missing keys:
   python3 -c "
import json
with open('JobbleBaby/app/i18n/en.json') as f: en = json.load(f)
with open('JobbleBaby/app/i18n/zh.json') as f: zh = json.load(f)
def get_keys(d, prefix=''):
    keys = []
    for k,v in d.items():
        full = prefix + k
        if isinstance(v, dict): keys.extend(get_keys(v, full+'.'))
        else: keys.append(full)
    return keys
en_keys = set(get_keys(en))
zh_keys = set(get_keys(zh))
for k in sorted(en_keys - zh_keys): print(k)
"
3. For each missing key, translate from English to Traditional Chinese and add to zh.json
4. Run TSC: cd JobbleBaby && npx tsc --noEmit — must pass 0 errors
5. Run pre-submission audit: node scripts/pre-submission-audit.js

### Files
- JobbleBaby/app/i18n/en.json (read only)
- JobbleBaby/app/i18n/zh.json (add missing translations)

### Done when:
- All en.json keys exist in zh.json
- TSC 0 errors
- Pre-submission audit PASS

**DONE ULW**
Repo: /mnt/c/Users/enoma/Desktop/opencode-work/agent-works/jobble-baby
