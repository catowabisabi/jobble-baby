# Sisyphus Task — Cycle 483
# Priority: P2
# Project: Jobble Baby

## TASK: Fix Hardcoded String Arrays → i18n Keys

**Keywords:** appetite_suppression_phase · acoustic_environment_scoring · binaural_interference

### Objective
Replace hardcoded string array enum values with i18n key references in the flagged files.

### Steps
1. Run pre-submission audit to get full list of hardcoded string files:
   cd /mnt/c/Users/enoma/Desktop/opencode-work/agent-works/jobble-baby/JobbleBaby && node scripts/pre-submission-audit.js
   Note: Look for the "Hardcoded Non-i18n Strings Detection" section.

2. For each flagged file, identify the hardcoded string arrays (enum values), add matching i18n keys to en.json and zh.json under an appropriate namespace (e.g. `enums.severity_levels`, `enums.body_areas`), then replace the hardcoded strings with i18n translations.

3. Run TSC after each file: cd JobbleBaby && npx tsc --noEmit — must pass 0 errors

4. After all files fixed, run full pre-submission audit — must still PASS

### Files to review (typically flagged):
- app/(tabs)/diaper-cream.tsx (severity levels, rash types, diaper areas)
- app/(tabs)/interoceptive.tsx (body areas)
- app/(tabs)/reflex-visual-motor.tsx (reflex states)
- app/(tabs)/regulatory-fitness.tsx (regulatory types)
- app/(tabs)/sleep-architecture.tsx (sound types, light levels, noise levels)
- (and others flagged by audit)

### i18n namespaces to create:
Add under `enums.*` in both en.json and zh.json:
- enums.severity: mild, moderate, severe
- enums.body_areas: head, throat, chest, abdomen, limbs, back, skin_folds
- enums.rash_types: yeast, contact, irritant, diaper_area
- enums.sound_types: whiteNoise, pinkNoise, brownNoise
- enums.light_levels: fullyDark, dim, nightLight
- enums.noise_levels: silent, veryQuiet, quiet
- (add more as needed per file)

### Done when:
- Pre-submission audit no longer flags hardcoded strings (or only legitimate cases remain)
- TSC 0 errors
- Both en.json and zh.json updated with all new enum keys

**DONE ULW**
Repo: /mnt/c/Users/enoma/Desktop/opencode-work/agent-works/jobble-baby
