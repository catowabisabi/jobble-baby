# Sisyphus Task — Cycle 456
# Priority: P2
# Project: Jobble Baby

## TASK: Console.log Sweep — Production Cleanup

**Keywords:** proprietary_drift · sleep_wake_cycle_stabilizer · visual_cortex_stimulation

### Objective
Remove all console.log / console.warn / console.debug statements from the app/ directory before production build. Pre-submission audit already flagged interoceptive.tsx:140. Do a full sweep.

### Steps
1. Run: `grep -rn "console\.\(log\|warn\|debug\)" JobbleBaby/app/ --include="*.ts" --include="*.tsx" -n`
2. For each match:
   - If it's a debug/development aid → remove entirely
   - If it might be useful for production errors → replace with comment or Sentry breadcrumb
3. Run TSC: `cd JobbleBaby && npx tsc --noEmit` — must pass 0 errors
4. Run pre-submission audit: `node scripts/pre-submission-audit.js`
5. Report files modified and line counts

### Files to Check (known problem areas)
- app/(tabs)/interoceptive.tsx (flagged line 140)
- Any other tab files with debug statements
- app/utils/, app/components/, app/contexts/ directories

### Done when:
- `grep -rn "console\.\(log\|warn\|debug\)" JobbleBaby/app/` returns 0 results
- TSC 0 errors
- Pre-submission audit PASS

**DONE ULW**
Repo: /mnt/c/Users/enoma/Desktop/opencode-work/agent-works/jobble-baby
